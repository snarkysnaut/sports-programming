#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n; cin >> n;
    int val = 0;
    for (int i = 0; i < n; ++i)
    {
        int v; cin >> v; val xor_eq v;
    }
    cout << val << '\n';
}